import { Component } from 'react';
import { SearchBar } from '../SearchBar';

export class Home extends Component {
  render() {
    return (
      <section>
        <SearchBar />
      </section>
    );
  }
}
